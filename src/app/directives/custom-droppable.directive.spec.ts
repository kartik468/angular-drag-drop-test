import { CustomDroppableDirective } from './custom-droppable.directive';

describe('CustomDroppableDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDroppableDirective();
    expect(directive).toBeTruthy();
  });
});
