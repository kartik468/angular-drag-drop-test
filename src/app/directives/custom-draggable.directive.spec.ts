import { CustomDraggableDirective } from './custom-draggable.directive';

describe('CustomDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
